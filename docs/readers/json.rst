JSON
============================================

In order to use ``JSONReader`` you need to install the ``jq`` package.

.. code-block:: bash

    pip install jq

.. automodule:: pineflow.readers.json
    :members:
